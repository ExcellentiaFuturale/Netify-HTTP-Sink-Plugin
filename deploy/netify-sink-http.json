{
    "timeout_connect": 30,
    "timeout_transfer": 300,
    "channels": {
        "netify-sink-legacy": {
            "enable": true,
            "timeout_connect": 30,
            "timeout_transfer": 300,
            "url": "https://sink.netify.ai/v1/",
            "headers": {
                "x-vendor-id": "EG",
                "x-uuid": "${uuid_agent}",
                "x-uuid-site": "${uuid_site}",
                "x-uuid-serial": "${uuid_serial}"
            }
        }
    }
}
